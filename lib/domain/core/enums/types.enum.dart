enum EventCategory { lecture, church, lecturer, other }

enum EventType {
  vision,
  pillar,
  foundational,
  tutorial,
  live,
  experience,
  encounter,
  assembly,
  other,
}

enum ScanType { scanIn, scanOut }
